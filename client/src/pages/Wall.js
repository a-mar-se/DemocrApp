import React from 'react';
import ShowAllPolls from '../components/ShowAllPolls.js';

const Wall = () => {
  return (
    <main className="page home">
      <ShowAllPolls />
    </main>
  );
};

export default Wall;
